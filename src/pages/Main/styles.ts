import { Button, CircularProgress } from '@material-ui/core'
import styled from 'styled-components'

export const ButtonMoreBooks = styled(Button)`
    height: 45px;
    background-color: #2d88ff;
    color: #FFF;
`;
export const Loading = styled(CircularProgress)`
   
    color: #2d88ff;
`;